import Fastify from "fastify"; import cors from "@fastify/cors"; import helmet from "@fastify/helmet"; import jwt from "@fastify/jwt"; import rateLimit from "@fastify/rate-limit"; import {env,corsOrigins} from "./config/env.js"; import {AppError} from "./utils/errors.js"; import {authRoutes} from "./modules/auth/auth.routes.js"; import {walletRoutes} from "./modules/wallet/wallet.routes.js"; import {gameRoutes} from "./modules/games/game.routes.js"; import {sessionRoutes} from "./modules/game-sessions/session.routes.js"; import {softAggregatorRoutes} from "./modules/providers/softaggregator.routes.js";
declare module "fastify"{interface FastifyInstance{authenticate:(request:import("fastify").FastifyRequest,reply:import("fastify").FastifyReply)=>Promise<void>}} declare module "@fastify/jwt"{interface FastifyJWT{payload:{sub:string};user:{sub:string}}}
export function buildApp(){const app=Fastify({logger:env.NODE_ENV!=="test"});app.register(helmet);app.register(cors,{origin:corsOrigins});app.register(rateLimit,{max:100,timeWindow:"1 minute"});app.register(jwt,{secret:env.JWT_SECRET,sign:{expiresIn:env.JWT_EXPIRES_IN}});app.decorate("authenticate",async(req,reply)=>{try{await req.jwtVerify()}catch{return reply.code(401).send({success:false,error:{code:"UNAUTHORIZED",message:"Authentication required"}})}});app.get("/health",async()=>({ok:true,service:"gaming-backend",environment:env.NODE_ENV}));app.register(authRoutes);app.register(walletRoutes); app.register(gameRoutes); app.register(sessionRoutes); app.register(softAggregatorRoutes);app.setErrorHandler((err,req,reply)=>{if(err instanceof AppError)return reply.code(err.statusCode).send({success:false,error:{code:err.code,message:err.message}});if(err.name==="ZodError")return reply.code(400).send({success:false,error:{code:"VALIDATION_ERROR",message:"Invalid request"}});req.log.error(err);return reply.code(500).send({success:false,error:{code:"INTERNAL_ERROR",message:"Internal server error"}})});return app;}
